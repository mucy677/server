1. Must connect to Dev container to make changes to code, and run mvn clean package to create server-0.0.1-SNAPSHOT.jar
2. Close remote connection to Dev container and run buildcontainer.sh in bash terminal
3. Commit any changes